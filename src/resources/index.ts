import {FrameworkConfiguration} from 'aurelia-framework';

//used for plainToClass
import 'reflect-metadata';

export function configure(config: FrameworkConfiguration): void {
  //config.globalResources([]);
}

